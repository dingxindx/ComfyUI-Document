# 节点速查表（中英文对照）

## 图像生成节点（通用）

| 中文名称 | 英文节点名 | 所在菜单路径 | 所属工作流 |
|:--------:|:----------:|:------------:|:---------:|
| 加载 Checkpoint | Load Checkpoint | `add_node → loaders → Load Checkpoint` | 图像/视频 |
| CLIP 文本编码器 | CLIP Text Encode (Prompt) | `add_node → conditioning → CLIP Text Encode (Prompt)` | 图像/视频 |
| 潜空间图像 | Empty Latent Image | `add_node → latent → Empty Latent Image` | 文生图 |
| KSampler | KSampler | `add_node → sampling → KSampler` | 图像/视频 |
| VAE 解码 | VAE Decode | `add_node → latent → VAE Decode` | 图像/视频 |
| 保存图像 | Save Image | `add_node → image → Save Image` | 文生图 |
| 加载图像 | Load Image | `add_node → image → Load Image` | ControlNet/IP-Adapter |
| OpenPose 预处理器 | OpenPose Preprocessor | 自定义节点 `controlnet_aux` | ControlNet |
| Canny 边缘 | Canny Edge | `add_node → image → preprocessing → Canny Edge` | ControlNet |
| ControlNet 加载器 | ControlNetLoader | `add_node → loaders → ControlNetLoader` | ControlNet |
| 应用 ControlNet | Apply ControlNet | `add_node → conditioning → Apply ControlNet` | ControlNet |
| CLIP Vision 加载器 | CLIPVisionLoader | `add_node → loaders → CLIPVisionLoader` | IP-Adapter |
| IP-Adapter 编码器 | IPAdapterEncoder | 自定义节点 `ComfyUI_IPAdapter_plus` | IP-Adapter |
| IP-Adapter 应用 | IPAdapterApply | 自定义节点 `ComfyUI_IPAdapter_plus` | IP-Adapter |
| LoRA 加载器 | Load LoRA | `add_node → loaders → Load LoRA` | 图像/视频 |

## LTX 视频生成节点

| 中文名称 | 英文节点名 | 所在菜单路径 | 所属工作流 |
|:--------:|:----------:|:------------:|:---------:|
| Checkpoint 加载器 | CheckpointLoaderSimple | 右键 → checkpoint | 文生/图生/两阶段 |
| Gemma 文本编码器加载器 | LTXTextEncoderLoader | 右键 → LTXText | 全部 |
| 空视频潜空间 | EmptyLTXVLatentVideo | 右键 → EmptyLTXVLatent | 全部 |
| LTX 文生视频采样器 | LTXVTextToVideoSampler | 右键 → LTXVTextToVideo | 文生/两阶段 |
| LTX 图生视频采样器 | LTXVImageToVideoSampler | 右键 → LTXVImageToVideo | 图生/首末帧 |
| LTX 潜空间放大 | LTXVLatentUpsampler | 右键 → LTXVLatentUpsampler | 两阶段 |
| 潜空间放大模型加载器 | LatentUpscaleModelLoader | 右键 → LatentUpscale | 两阶段 |
| LoRA 加载器（仅模型） | LoraLoaderModelOnly | 右键 → LoraLoader | 两阶段 |
| 分块 VAE 解码 | VAEDecodeTiled | 右键 → VAEDecodeTiled | 全部 |
| Unet 加载器（GGUF） | Unet Loader (GGUF) | 右键 → GGUF → Unet Loader | GGUF |
| 保存视频 | SaveVideo | 右键 → SaveVideo | 全部 |
| RTX 视频超分辨率 | RTX Video Super Resolution | 右键 → RTX Video | 优化 |

## Wan 2.2 节点

| 中文名称 | 英文节点名 | 说明 |
|:--------:|:----------:|:----:|
| UNET 加载器 | UNETLoader | Wan 专用分体加载 |
| VAE 加载器 | VAELoader | Wan 专用 VAE |
| SD3 模型采样调度器 | ModelSamplingSD3 | Wan 使用 SD3 调度方式 |
| VAE 编码 | VAE Encode | I2V 首帧编码 |
| 潜空间裁剪 | LatentCut | 自定义节点 `ComfyUI-KJNodes` |
| 潜空间拼接 | LatentConcat | I2V 首帧过烤修复 |
